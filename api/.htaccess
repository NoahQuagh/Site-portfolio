RewriteEngine On
# Redirige tout vers index.php en passant la route en paramètre
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?route=$1 [QSA,L]
Order deny,allow
Deny from all